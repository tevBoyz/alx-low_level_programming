#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a given string
 * @s: string given
 *
 * Return: string length int
 */

int _strlen(char *s)
{
int n;

n = strlen(s);
return (n);
}

